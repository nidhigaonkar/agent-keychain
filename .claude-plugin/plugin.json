{
  "name": "agent-keychain",
  "version": "0.2.0",
  "description": "Lets a coding agent top up its own API credits when it runs low, using Stripe Link for payment — no Stripe account or API keys required.",
  "author": {
    "name": "Nidhi Gaonkar"
  },
  "repository": "https://github.com/nidhigaonkar/agent-keychain",
  "license": "MIT",
  "keywords": ["stripe", "link", "api-credits", "billing", "agent"],
  "skills": "./skills",
  "mcpServers": {
    "link-cli": {
      "command": "npx",
      "args": ["@stripe/link-cli", "--mcp"],
      "description": "Stripe Link CLI — handles wallet, spend requests, and approval flow for Agent Keychain."
    }
  }
}
