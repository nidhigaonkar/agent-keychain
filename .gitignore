node_modules/
.DS_Store
*.log
.env

# Local, machine-specific config generated by `npm run setup` — don't ship
# the maintainer's own MCP wiring or skill copy to people who clone this repo.
.claude/

# Local incident/debug logs — may contain real account/run details, never ship.
logs/
